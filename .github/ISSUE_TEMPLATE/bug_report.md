---
name: Bug report
about: Report a problem with the bridge
title: "[bug] "
labels: bug
---

**Describe the bug**
A clear description of what's wrong.

**To reproduce**
Steps, including the relevant env (`OBSIDIAN_SINK`, `VAULT_DIR`, `SUMMARIZE_PLATFORMS`, etc.) and what the poll loop was doing.

**Logs**
Paste the `obsidian-vexa-bridge` container logs around the issue.
⚠️ **Redact your `DATABASE_URL`, `OBSIDIAN_MCP_TOKEN`, LLM API key, and any credentials** before pasting.

**Environment**
- Bridge version / image tag:
- Vexa version:
- `OBSIDIAN_SINK` (`fs` or `mcp`):
- LLM endpoint and `AI_MODEL`:
- Host (NAS / server / OS):

**Expected behavior**
What you expected to happen.
